// Test new preset naming (recommended)
import { libraryPreset } from '@eslint-sample/eslint-config';

// Test backward compatibility (still works)
// import { libraryConfig } from '@eslint-sample/eslint-config';

// Test direct preset import for better tree shaking
// import { libraryPreset } from '@eslint-sample/eslint-config/library';

export default libraryPreset;
